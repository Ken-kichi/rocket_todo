<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/uikit.min.css" />
    <script src="js/uikit.min.js"></script>
    <script src="js/uikit-icons.min.js"></script>
    <title>TODO一覧</title>
</head>

<body>
    <nav class="uk-navbar-container uk-margin">
        <div class="uk-navbar-left">
            <a class="uk-navbar-item uk-logo" href="/">TODOアプリ</a>
        </div>
    </nav>

    <div class="uk-navbar-right uk-position-top-right uk-margin-small-top" style="margin-right: 100px;top:10px">
        <ul class="uk-navbar-nav">
            <li>
                <button href="/add" class="uk-button uk-button-primary">TODOを追加</button>
            </li>
        </ul>
    </div>

    <div class="uk-container">
        <h1 class="uk-heading-line uk-text-center"><span>TODO一覧</span></h1>
        <div class="uk-child-width-1-2@s uk-child-width-1-3@m uk-child-width-1-4@l uk-grid-match" uk-grid>
            {%if message%}
            <div class="uk-width-1-1">
                <div class="uk-card uk-card-default uk-card-body">
                    <h3 class="uk-card-title">{{message}}</h3>
                </div>
            </div>
            {% else %}
            {% for todo in todos%}
            <div>
                <div class="uk-card uk-card-default uk-card-body">
                    <h3 class="uk-card-title">{{todo.title}}</h3>
                    {% if todo.completed %}
                    <p>完了</p>
                    {% else %}
                    <p>未完了</p>
                    {% endif %}
                    <a href="/update/{{todo.id}}" class="uk-button uk-button-default">更新</a>
                    <a href="/delete/{{todo.id}}" class="uk-button uk-button-danger">削除</a>
                </div>
            </div>
            {% endfor %}
            {% endif%}
        </div>
    </div>
</body>


</html>
